version: '3.9'

services:
  postgres:
    image: postgres:16.4-alpine
    ports:
      - "5433:5432"
    environment:
      - POSTGRES_PASSWORD=notarealpassword
      - POSTGRES_USER=postgres
      - POSTGRES_DB=prsdlocal