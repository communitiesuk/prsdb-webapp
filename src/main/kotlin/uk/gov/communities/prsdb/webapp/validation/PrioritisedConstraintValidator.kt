package uk.gov.communities.prsdb.webapp.validation

/**
 * Marker interface for classes that can be used as a validator by a ConstraintDescriptor. In practice, all such
 * classes must extend one of PrioritisedConstraintValidator's children.
 */
interface PrioritisedConstraintValidator
